//need some code here :p
describe('description', function() {
  assertEquals(divide(4), true);
  assertEquals(divide(2), false);
  assertEquals(divide(5), false);
  assertEquals(divide(88), true);
  assertEquals(divide(100), true);
  assertEquals(divide(67), false);
  assertEquals(divide(90), true);
  assertEquals(divide(10), true);
  assertEquals(divide(99), false);
  assertEquals(divide(32), true);
});
